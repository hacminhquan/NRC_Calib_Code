# --- Core ML stack ---
# pytorch_lightning>=2 and pyro-ppl are what the real QRT/PCS repos import
# directly (verified by cloning both repos) -- NOT the newer unified
# `lightning` package, whose import path (lightning.pytorch.*) differs.
torch>=2.2
pytorch_lightning>=2
pyro-ppl
numpy>=1.26
scipy>=1.12
pandas>=2.2
scikit-learn>=1.4

# --- Dataset sources used by the real datamodules ---
# NOTE: both real repos pin openml==0.13.1 exactly. Verified by actually
# running the download path in this project's sandbox: 0.13.1 breaks under
# NumPy 2.0 (uses the removed `np.sctypes` attribute). Using a recent
# version instead -- confirmed working (reaches the real network call
# correctly) with numpy>=1.26/2.x here.
openml>=0.14
dask[complete]         # used by their preprocessing pipeline
numba                 # used by their preprocessing pipeline
xlrd
openpyxl

# --- Viz ---
matplotlib>=3.8
plotly>=5.20
seaborn>=0.13
tueplots               # used by their own figure styling
colorcet

# --- Experiment / config management ---
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.16

# --- Utilities ---
tqdm>=4.66
einops>=0.7
pyyaml>=6.0
rich>=13.7
psutil>=5.9
ngboost                # optional baseline used in some of their comparisons

# --- Notebook environment ---
jupyterlab>=4.1
ipywidgets

# --- Testing (dev) ---
pytest>=8.0
